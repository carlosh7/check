# CONFIGURACIÓN DEL PROYECTO - CHECK PRO

## 📍 RUTAS IMPORTANTES

### Proyecto principal (donde hacer cambios)
```
C:\Users\carlo\OneDrive\Documentos\APP\Registro
```

### Container Docker (para pruebas)
```
C:\Users\carlo\check
```

---

## ⚠️ REGLA DE ORO (OBLIGATORIA)

**AL FINAL DE CADA TAREA/TICKET:**
```bash
git add .
git commit -m "mensaje"
git push origin main
```

**SIEMPRE ejecutar, sin excepción.**

---

## 🔧 COMANDOS ÚTILES

### Navegar al proyecto
```bash
cd "C:\Users\carlo\OneDrive\Documentos\APP\Registro"
```

### Iniciar servidor
```bash
cd "C:\Users\carlo\OneDrive\Documentos\APP\Registro"
node server.js
```

### Reiniciar servidor (detener y arrancar)
```bash
powershell -Command "Stop-Process -Name 'node' -Force -ErrorAction SilentlyContinue"
node server.js
```

### Verificar que el servidor corre
```bash
curl -s http://localhost:3000/
```

### Probar login
```bash
curl -s http://localhost:3000/api/login -X POST -H "Content-Type: application/json" -d "{\"username\":\"admin@check.com\",\"password\":\"admin123\"}"
```

### Instalar dependencias
```bash
cd "C:\Users\carlo\OneDrive\Documentos\APP\Registro"
npm install
```

---

## 🔐 CREDENCIALES

- **Usuario admin:** admin@check.com
- **Contraseña:** admin123
- **Puerto servidor:** 3000

---

## 📝 NOTAS

- El servidor debe estar corriendo para probar cambios
- Verificar que no haya errores en consola antes de hacer commit
- El container Docker (`C:\Users\carlo\check`) es independiente - NO confundir con el proyecto

---

## 🏷️ TAGS PARA RECORDAR

`proyecto_principal`, `check_pro`, `registro_eventos`, `node_express`
